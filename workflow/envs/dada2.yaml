channels:
  - conda-forge
  - bioconda
  - r
  - defaults
dependencies:
  - r-base=4.2  # R version 4.2
  - r-biocmanager=1.30.18  # BiocManager version compatible with Bioconductor 3.16
  - libtiff=4.4.0  # Choose a specific version of libtiff
  - libdeflate=1.14  # Choose a specific version of libdeflate to avoid conflicts
  - zlib
  - libpng
  - libjpeg
  - gfortran
  - libxml2
  - cairo
  - pango
  - fontconfig
  - freetype
  